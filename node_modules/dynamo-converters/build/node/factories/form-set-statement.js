"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createFormSetStatement = void 0;

const createFormSetStatement = (convertDataValue, createPropertyName, isIllegalWord) => (property, value, expressionAttributeNames, expressionAttributeValues) => {
  if (isIllegalWord(property)) {
    const propertyName = createPropertyName(property, expressionAttributeNames);
    expressionAttributeValues[`:${propertyName}`] = convertDataValue(value);
    return `#${propertyName} = :${propertyName}`;
  }

  expressionAttributeValues[`:${property}`] = convertDataValue(value);
  return `${property} = :${property}`;
};

exports.createFormSetStatement = createFormSetStatement;